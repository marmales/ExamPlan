@model string
<html>
    <head>
        <title>Exam Manager</title>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <style>
            body {
                color: #d3d3d3;
                font: 12pt arial;
                background-color: #222222;
            }
            #mynetwork {
                width: 100%;
                height: 600px;
                background-color: #222222;
            }

            table, tr, td {
                border: 1px solid white;
            }

            #terminarz {
                margin-left: auto;
                margin-right: auto;
            }
        </style>

        <script type="text/javascript" src="~/Scripts/vis.js"></script>
        <script type="text/javascript" src="~/Scripts/tables.js"></script>
        <link href="~/Content/vis.css" rel="stylesheet" type="text/css" />
    </head>

    <body>

        <h2 align="center">Graf konfliktów między poszczególnymi egzaminami:</h2>

        <div id="mynetwork"></div>


        <script type="text/javascript">
        var color = 'gray';
        var len = undefined;


        // create a network
        var container = document.getElementById('mynetwork');
        var data = {
            nodes: nodes,
            edges: edges
        };
        var options = {
            nodes: {
                shape: 'dot',
                size: 30,
                font: {
                    size: 32,
                    color: '#ffffff'
                },
                borderWidth: 2
            },
            edges: {
                width: 2
            }
        };
        network = new vis.Network(container, data, options);


        </script>

    </body>
    </html>
      